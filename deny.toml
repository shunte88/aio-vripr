# cargo-deny configuration (D7).
#
# The point of this file is licence drift: VCW's own code is MIT, and the moment a
# dependency bump pulls in something copyleft the binaries' obligations change
# without anyone noticing. CI fails the build instead.
#
# Run locally with: cargo deny check

[graph]
all-features = true
targets = [
    "x86_64-unknown-linux-gnu",
    "aarch64-unknown-linux-gnu",
    "x86_64-pc-windows-msvc",
    "aarch64-apple-darwin",
]

[advisories]
version = 2
yanked = "deny"
ignore = []

[licenses]
version = 2
# Permissive, attribution-only licences. Anything outside this list is a decision,
# not an accident, and has to be added here with a reason.
allow = [
    "0BSD",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "BSL-1.0",
    "CC0-1.0",
    "CDLA-Permissive-2.0",
    "ISC",
    "MIT",
    "MIT-0",
    "MPL-2.0",
    "Unicode-3.0",
    "Unlicense",
    "Zlib",
]
confidence-threshold = 0.93

# Phase 2 will add exactly one weak-copyleft dependency, deliberately and with the
# relink obligation documented in THIRD-PARTY-NOTICES.md. Uncomment when
# chromaprint-next actually lands in the dependency graph, and not before - an
# allowance carried ahead of the dependency is an allowance nobody reviews.
#
# [[licenses.exceptions]]
# name = "chromaprint-next"
# allow = ["LGPL-2.1-or-later"]

[bans]
multiple-versions = "warn"
wildcards = "deny"
deny = []

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
