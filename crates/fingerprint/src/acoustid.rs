//! AcoustID lookup (§26, §27).
//!
//! Filled at Phase 2, and it needs an API key. §39 forbids credentials in project
//! files, so the key comes from the OS keychain or the environment, never from
//! anything the repository or a `.vcw` file carries.
