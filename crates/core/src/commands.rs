//! The command surface the UI drives the core through (§35).
//!
//! Filled by WP-07. Types here are the source of the generated TypeScript (D9);
//! CI fails on drift rather than trusting anyone to keep two definitions in step.
